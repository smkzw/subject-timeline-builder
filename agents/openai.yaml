interface:
  display_name: "Subject Timeline Builder"
  short_description: "Build clinical subject timeline HTML"
  default_prompt: "Use $subject-timeline-builder to build a Chinese interactive subject timeline from the protocol, data listings, findings, and PK data."
